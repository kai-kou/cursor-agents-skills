`/Users/kai.ko/dev/` のプロジェクト一覧を最新状態に同期してください。

以下を実行してください：
1. 各ステータスフォルダ（01_active〜05_archive）を走査
2. 各プロジェクトのREADME.mdからメタデータを収集
3. `_management/PROJECT_STATUS.md` のテーブルを実際のフォルダ内容に合わせて更新
4. サマリーの件数を再計算
5. 変更履歴に記録
